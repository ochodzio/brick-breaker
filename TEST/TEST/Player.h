#pragma once
#include <vector>
#include <time.h>
#include <fstream> // added for file handling
#include <string>// adde for string manipulation
#include <iostream>
using namespace std;
class Player
{
public:
	string name;
	int score;
	Player(string tname, int tscore);

};

